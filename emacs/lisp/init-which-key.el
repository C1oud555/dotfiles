;;; init-which-key.el --- Interactive completion in buffers -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:
(use-package which-key
  :init (which-key-mode))

(provide 'init-which-key)
;; init-which-key.el ends here
