# -*- mode: snippet -*-
# name: always ff block
# key: aff
# --
always_ff @(posedge ${1:clk} or negedge ${2:rst_n}) begin
  if (!$2) begin
    ${3:reg_name} <= 'b0;
  end else begin
    $3 <= $4;
  end
end$0
